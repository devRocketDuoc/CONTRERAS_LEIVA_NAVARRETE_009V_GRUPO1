spring.application.name=payment-service
server.port=8089
spring.profiles.active=local

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE

springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.path=/doc/swagger-ui.html

services.trip.url=${SERVICES_TRIP_URL:http://localhost:8086}
services.reservation.url=${SERVICES_RESERVATION_URL:http://localhost:8087}

app.jwt.secret=${APP_JWT_SECRET:airflytrip-2026-jwt-secret-development-key-for-local-microservices}
